const SearchField = () => {
  return (
    <div>
      <form action="">
        <input
          type="text"
          placeholder="Search for chats"
          className="px-[20px] py-[12px] block w-full bg-gray-50 border border-gray-300 text-gray-900 rounded-full focus:outline-gray-500"
        />
      </form>
    </div>
  );
};

export default SearchField;
